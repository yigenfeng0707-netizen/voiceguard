{
  "platform": "TraeWork",
  "priority": "P1",
  "mode": "http_api",
  "api_url": "http://127.0.0.1:8765/v1/qa",
  "fast_api_url": "http://127.0.0.1:8765/v1/qa/fast",
  "health_url": "http://127.0.0.1:8765/health",
  "method": "POST",
  "audio_field": "audio",
  "params": {
    "packs": "all",
    "device": "GPU"
  },
  "triggers": [
    "通话质检",
    "合规质检",
    "录音质检",
    "违规话术检测",
    "红线词检测"
  ],
  "supported_formats": ["wav", "mp3", "m4a", "flac"],
  "timeout_seconds": 180,
  "response_format": "json",
  "notes": "HTTP API 模式：复用初赛 TraeWork 接入模板，改路由为 /v1/qa。需先启动 scripts/server.py。配置触发词后上传音频到 API 接口，返回 JSON 质检报告（含评分/违规清单/漏斗/token经济性）。"
}
